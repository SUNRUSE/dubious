import * as types from "./types"
import sprite from "./sprite"
import background from "./background"
import sound from "./sound"
import song from "./song"

const exported: types.PurposeImplementation = {
  sprite,
  background,
  sound,
  song
}

export default exported
